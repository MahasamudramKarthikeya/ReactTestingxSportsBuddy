// app.js
import ReactDOM from "react-dom/client";
import App from "./App.jsx"; // Ensure this points to your React component file

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
